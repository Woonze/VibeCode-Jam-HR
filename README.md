# 📘 **README.md — AI HR Interview System**

AI HR Interview System — это интерактивная система технического интервью с кандидатом, включающая:

* 🤖 **ИИ-рекрутера**, который ведёт диалог, выдаёт 3 задачи, анализирует решения и формирует отчёт
* 🧪 **Песочницу (Node.js Sandbox)** для безопасного выполнения JavaScript-кода
* 📊 **LLM-оценку** решений кандидата
* 📄 **Генерацию красивого PDF-отчёта**
* 💻 **Веб-интерфейс (React + Monaco Editor)** для взаимодействия с системой
* ⚙️ **Бэкенд (FastAPI)**

Система полностью автономна и имитирует реальное техническое интервью в реальном времени.

---

# 📂 **Структура проекта**

```
ai-hr-interviewSystem/
│
├── backend/
│   ├── main.py                # основной backend-сервер FastAPI
│   ├── models.py              # pydantic-модели запросов/ответов
│   ├── sandbox.py             # Node.js sandbox (выполнение кода кандидата)
│   ├── llm_client.py          # логика вызова LLM и анализа решений
│   ├── report.py              # генерация PDF отчёта
|   ├── requirements.txt
│   ├── fonts/                 # Roboto-Regular.ttf, Roboto-Bold.ttf
│   └── reports/               # Генерируемые PDF
│
├── frontend/
│   ├── public/
│   │   └── content/FoturLogoSquare.png
│   ├── src/components
│   │   ├── Workspace.tsx      # основной интерфейс системы
│   │   ├── Workspace.css      # UI дизайн
|   ├── index.html
│   ├── package.json
│   ├── vite.config.js
│   └── tsconfig.json
│
└── README.md (этот файл)
```

---

# ⚙️ Установка и запуск

## 1️⃣ Установка backend

Перейдите в папку `backend`:

```bash
cd backend
```

Установите зависимости:

```bash
pip install -r requirements.txt
```

Запуск сервера:

```bash
uvicorn main:app --reload
```

Сервер запустится на:

```
http://127.0.0.1:8000
```

---

## 2️⃣ Установка frontend

Перейдите в папку `frontend`:

```bash
cd frontend
```

Установите зависимости:

```bash
npm install
```

Запуск dev-сервера:

```bash
npm run dev
```

По умолчанию:

```
http://localhost:5173
```

---

# 🧠 Как работает интервью

Процесс состоит из 3 задач:

1. 🟩 **Лёгкое задание**
2. 🟨 **Среднее по сложности**
3. 🟥 **Сложное финальное**

ИИ:

* выдаёт каждую задачу
* анализирует решение
* подбирает следующую задачу, учитывая сильные/слабые стороны
* сохраняет историю выполнения кода
* ведёт список всех сообщений
* формирует итоговый PDF-отчёт кандидата

---

# 🧩 Backend

## **main.py**

Основной сервер FastAPI:
✔ роуты API
✔ выдача задач
✔ сохранение истории
✔ анализ решений
✔ формирование итогового отчёта

---

# ✉️ API — Полный список эндпоинтов

## **POST /api/start_interview**

Начало интервью.

Ответ:

```json
{
  "messages": [...],
  "task": { "id": "...", "title": "...", "description": "...", "template": "..." }
}
```

---

## **POST /api/compile**

Запуск кода в Node.js sandbox.

Тело запроса:

```json
{
  "code": "console.log(123)",
  "language": "javascript",
  "taskId": "easy-1"
}
```

Ответ:

```json
{
  "stdout": "123\n",
  "stderr": "",
  "metrics": { "exec_time_ms": 12 }
}
```

---

## **POST /api/submit**

Отправка решения задачи.

Ответ когда задача НЕ последняя:

```json
{
  "messages": [...],
  "task": {...},
  "finished": false
}
```

Ответ после третьей задачи:

```json
{
  "messages": [...],
  "report": "/reports/report_Candidate_1_abc123.pdf",
  "finished": true
}
```

---

## **GET /api/messages**

Получить чат за всё интервью.

---

# 🧠 LLM анализатор (`llm_client.py`)

ИИ получает:

* описание задачи
* код кандидата
* результат выполнения
* историю ошибок

Возвращает:

```json
{
  "score": 72,
  "comment": "Хорошая логика, но есть проблемы со входными данными",
  "issues": [
    { "type": "performance", "detail": "Можно улучшить Big-O" }
  ],
  "summary": "Кандидат уверен в основах JS..."
}
```

---

# 🧪 Песочница (`sandbox.py`)

Изолированный запуск JS:

* создаёт временный файл
* запускает через Node.js
* убивает процесс через 3 секунды
* декодирует вывод **строго UTF-8**
* очищает мусор

---

# 📄 PDF-отчёт (`report.py`)

Формирует отчёт:

* профиль кандидата
* история попыток
* решения по каждой задаче
* вывод песочницы
* анализ LLM
* итоговая оценка

Отчёт создаётся в папке:

```
backend/reports/
```

---

# 📘 Банк задач (`входит в main.py`)

Содержит:

* 10 лёгких задач
* 10 средних
* 10 сложных

Все с:

* id
* title
* description
* шаблон кода

---

# 🎨 Frontend

Основной файл:

```
frontend/src/components/Workspace.tsx
```

Компонент включает:

* монокредактор
* чат с ИИ
* лог песочницы
* подробный UX
* два режима кнопок
* адаптивный интерфейс

Стили:

```
Workspace.css
```

---

# 🚀 Релиз и деплой

## **Backend — Docker**

Пример Dockerfile:

```Dockerfile
FROM python:3.11
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

---

## **Frontend — Vite build**

```bash
npm run build
```

Файлы попадут в папку `dist`.