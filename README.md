# 📘 **README.md — AI HR Interview System**

# 📹 **ВИДЕО-ДЕМО** https://vkvideo.ru/video357211139_456239446?list=ln-g35R2RMOa4AhLszlpj&uh=8511c5350bff62&sh=4

AI HR Interview System — это интерактивная система технического интервью с кандидатом, включающая:

* 🤖 **ИИ-рекрутера**, который ведёт диалог с кандидатом как ИИ-рекрутер
* 💻 **Блоки** с 3 техническими задачами (JS или Python) + блок soft-skills
* 🧪 **Песочницу** которая запускает код кандидата в изолированной среде и прогоняет автотесты
* 📊 **LLM-оценку** решений и ответов (качество кода, коммуникации, soft-skills) 
* 📄 **Генерацию красивого и детальный PDF-отчёта**
* 💻 **Веб-интерфейс (React + Monaco Editor)** для взаимодействия с системой
* 🕵️ **Античит**, который отслеживает подозрительные действия (копипаст, переключение вкладок)

Фронтенд: **React + TypeScript + Vite + Monaco Editor**  
Бэкенд: **FastAPI (Python)** + **Node.js** для JS-песочницы.

Система полностью автономна и имитирует реальное техническое интервью в реальном времени.

---

# 📂 **Структура проекта**

```
ai-hr-interview-system/
│
├── backend/
│   ├── main.py              # основной FastAPI-приложение (все эндпоинты)
│   ├── models.py            # Pydantic-модели запросов/ответов
│   ├── sandbox.py           # запуск JS/Python-кода кандидата (песочница)
│   ├── llm_client.py        # вызовы LLM и функции анализа
│   ├── report.py            # генерация PDF-отчёта (ReportLab)
│   ├── tests_runner.py      # запуск автотестов по банку задач
│   ├── TASK_BANK_JS.py      # банк задач по JavaScript
│   ├── TASK_BANK_PY.py      # банк задач по Python
│   ├── TEST_BANK_JS.py      # банк тестов для JS-задач
│   ├── TEST_BANK_PY.py      # банк тестов для Python-задач
│   ├── SOFT_SKILLS_BANK.py  # сценарии soft-skills интервью
│   ├── fonts/
│   │   ├── Roboto-Regular.ttf
│   │   └── Roboto-Bold.ttf
│   ├── reports/             # сюда сохраняются PDF-отчёты
│   ├── requirements.txt     # зависимости backend
│   └── .env                 # настройки LLM / ключи (создаётся вручную)
│
├── frontend/
│   ├── public/
│   │   └── content/
│   │       └── FoturLogoSquare.png
│   ├── src/
│   │   ├── assets/          # шрифты/изображения для фронта
│   │   ├── components/
│   │   │   ├── Workspace.tsx  # основной рабочий интерфейс интервью
│   │   │   └── Workspace.css  # стили рабочего места
│   │   ├── App.tsx
│   │   ├── App.css
│   │   ├── index.css
│   │   └── main.tsx
│   ├── index.html
│   ├── package.json
│   ├── tsconfig.json
│   ├── tsconfig.app.json
│   ├── tsconfig.node.json
│   ├── postcss.config.js
│   └── tailwind.config.js   # если используется Tailwind (подключен в проекте)
│
└── README.md

```

---
# 🔧 Требования

- Python ≥ 3.11
- Node.js ≥ 18 (для Vite и JS-песочницы)
- npm или pnpm / yarn
-(опционально) Docker и docker-compose для контейнеризации
-Доступ к LLM-API (ключи указываются в backend/.env — см. llm_client.py)

---

## ⚙️ Запуск проекта локально (без Docker)

### 1. Backend

Перейти в папку backend:

```bash
cd backend
```

Создать и активировать виртуальное окружение (рекомендуется):

```bash
python -m venv .venv
# Windows:
.venv\Scripts\activate
# macOS / Linux:
source .venv/bin/activate
```

Установить зависимости:

```bash
pip install -r requirements.txt
```

Создать файл `.env` в `backend/` — пример:

```env
# Пример, реальные имена переменных смотрите в llm_client.py
OPENAI_API_KEY=ваш_ключ
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=qwen3-32b-awq
```

Запустить FastAPI-сервер:

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

Бэкенд будет доступен по адресу:

```text
http://127.0.0.1:8000
```

> CORS в `main.py` настроен на `allow_origins=["*"]`, так что фронтенд с другого порта (Vite) будет работать без доп. настроек.

---

### 2. Frontend

В другом терминале:

```bash
cd frontend
npm install
```

Запуск dev-сервера Vite:

```bash
npm run dev
```

По умолчанию Vite стартует на:

```text
http://localhost:5173
```

Фронт обращается к бэкенду через относительный путь `/api/...`, поэтому при локальной разработке удобно настроить прокси в `vite.config.*` (пример):

```ts
// vite.config.ts (пример)
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
    },
  },
},
```

Тогда запросы из фронта (`fetch("/api/submit")`) будут автоматически проксироваться на FastAPI.

---

## 🐳 Запуск через Docker

Ниже пример, как можно завернуть проект в Docker.
Если у тебя свои Dockerfile / docker-compose — можно ориентироваться на эти команды.

### Backend в Docker

Пример `Dockerfile` для `backend/`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Системные зависимости при необходимости (например, для reportlab)
RUN apt-get update && apt-get install -y \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Важно: fonts/ и reports/ упакованы внутрь
ENV PYTHONUNBUFFERED=1

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

Сборка образа и запуск:

```bash
cd backend
docker build -t ai-hr-backend .
docker run --env-file .env -p 8000:8000 ai-hr-backend
```

### Frontend в Docker (build + static)

Пример `Dockerfile` для `frontend/`:

```dockerfile
# Stage 1: build
FROM node:20-alpine AS build

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# Stage 2: nginx
FROM nginx:alpine

COPY --from=build /app/dist /usr/share/nginx/html

# Проксируем /api на backend (пример конфигурации)
# Можно смонтировать свой nginx.conf через volume или COPY.
```

Сборка и запуск:

```bash
cd frontend
docker build -t ai-hr-frontend .
docker run -p 8080:80 ai-hr-frontend
```

В проде обычно настраивается nginx так, чтобы:

* статика фронта отдавалась по `/`
* все запросы `/api/*` проксировались на контейнер с backend (`http://backend:8000`)

---

### Пример `docker-compose.yml` (для dev/stage)

```yaml
version: "3.9"

services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
```

При таком варианте важно, чтобы фронт либо:

* обращался к `/api/...` и nginx внутри образа фронта проксировал на `backend:8000`,
* либо в сборке фронта была указана переменная окружения с базовым URL API.

---

## 🧠 Логика интервью

### Этапы

1. **Выбор направления**
   Пользователь в UI выбирает: **JavaScript** или **Python**
   → `POST /api/select_track` с `{ "track": "js" | "python" }`

2. **Старт интервью**
   → `POST /api/start_interview`
   Возвращает приветственные сообщения и **первую лёгкую задачу**.

3. **Техническая часть (3 задачи)**

   * Лёгкая (`easy`)
   * Средняя (`med`)
   * Сложная (`hard`)

   Для каждой задачи:

   1. Кандидат пишет код справа в Monaco Editor.

   2. Может нажимать **«Запустить в песочнице»** → `POST /api/compile`

   3. Когда готов — нажимает **«Готово — отправить»** → `POST /api/submit`

   4. Бэкенд:

      * прогоняет код через автотесты (`tests_runner.py` + `TEST_BANK_*.py`)
      * отправляет контекст в LLM (`analyze_code`)
      * сохраняет результат в `session["pendingResult"]`
      * задаёт **вопрос по коммуникации** (пояснить решение)

   5. UI переводится в режим ожидания ответа; кандидат пишет текстовый ответ и жмёт «Ответить» → `POST /api/communication_answer`

   6. Бэкенд:

      * анализирует ответ через LLM (`analyze_communication`)
      * считает итоговый балл задачи: 70% код, 30% коммуникация
      * записывает в `session["results"]`
      * либо выдаёт следующую задачу, либо переходит к soft-skills.

4. **Soft-skills блок**

   После 3-й задачи:

   * `session["soft_stage"] = True`
   * выбирается несколько сценариев из `SOFT_SKILLS_BANK.py`
   * фронтенд переключается в Soft-режим:

     * редактор кода скрыт
     * справа показывается текстовая карточка ситуации и подсказка
   * кандидат отвечает в текстовом поле → `POST /api/soft_answer`
   * LLM (`analyze_soft_skill`) выставляет оценки:

     * communication / teamwork / adaptability / leadership / problem_solving
   * после всех сценариев рассчитываются агрегаты soft-skills.

5. **Финальное резюме + отчёт**

   Функция `build_final_summary(...)` (в `main.py`):

   * считает:

     * средний балл за код
     * средний балл за коммуникацию по задачам
     * агрегированные soft-skills
     * интегральный балл кандидата
   * оценивает риск списывания (по результатам античита)
   * формирует рекомендации и вердикт:

     * «Рекомендован»
     * «Условно рекомендован»
     * «Не рекомендован»
   * запрашивает у LLM:

     * strengths
     * weaknesses
     * recommendations
     * summary

   Затем `report.py` генерирует красивый **PDF-отчёт** в `backend/reports/`.

---

## 🔍 API эндпоинты

### Трек и старт

* **POST `/api/select_track`**
  Выбор направления: `{ "track": "js" | "python" }`

* **POST `/api/start_interview`**
  Старт интервью, выдаёт приветствие и первую задачу.

---

### Песочница и задачи

* **POST `/api/compile`**
  Запуск кода в песочнице (JS или Python):

  ```json
  {
    "code": "function x() { return 1 }",
    "language": "javascript",
    "taskId": "js-easy-1"
  }
  ```

  Ответ:

  ```json
  {
    "stdout": "1\n",
    "stderr": "",
    "metrics": {
      "exec_time_ms": 12
    }
  }
  ```

* **POST `/api/submit`**
  Кандидат нажал «Готово» по задаче.

  Тело:

  ```json
  {
    "taskId": "js-easy-1",
    "code": "function sortNumbers(...) { ... }",
    "runResult": { ... }  // опционально: результат последнего запуска
  }
  ```

  Ответ включает:

  ```json
  {
    "messages": [...],                // обновлённый чат
    "ask_communication": true,        // нужно задать ответ на уточняющий вопрос
    "communication_question": "...",  // вопрос по решению
    "tests": [                        // результаты тестов
      { "name": "Тест #1", "passed": true, "visible": true },
      ...
    ],
    "tests_score": 100,
    "score_code": 85,
    "comment_code": "Комментарий LLM...",
    "finished": false                 // true только после финала интервью
  }
  ```

---

### Коммуникации и soft-skills

* **POST `/api/communication_answer`**
  Ответ кандидата на уточняющий вопрос по задаче.
  Бэкенд возвращает новый кусок анализа и, при необходимости, следующую задачу или переход к soft-skills.

* **POST `/api/soft_answer`**
  Ответ кандидата на soft-skills ситуацию:

  ```json
  {
    "taskId": "soft-1",
    "answer": "Я бы сделал так..."
  }
  ```

  Возвращает либо следующий soft-вопрос, либо финал интервью и путь к PDF-отчёту.

---

### Античит

* **POST `/api/anti_cheat_event`**
  Фронтенд отправляет события:

  ```json
  {
    "eventType": "paste" | "tab_switch" | "window_blur",
    "timestamp": 1713700000000,
    "taskId": "js-med-1"
  }
  ```

* **POST `/api/anti_cheat_analyze`**
  Периодические снимки кода для анализа списывания:

  ```json
  {
    "taskId": "js-med-1",
    "code": "function ...",
    "codeLength": 123,
    "timestamp": 1713700000000,
    "taskDescription": "..."
  }
  ```

  В ответ приходит:

  ```json
  {
    "ok": true,
    "analysis": {
      "cheating_probability": 20,
      "risk_level": "low",
      "suspicious_events": [...]
    }
  }
  ```

---

### Прочее

* **GET `/api/messages`**
  Отладочный эндпоинт — возвращает текущий чат целиком.

---

## 🧪 Автотесты и банк задач

Банк задач разбит по трём уровням:

* `TASK_BANK_JS.py` / `TASK_BANK_PY.py`:

  ```python
  TASK_BANK_JS = {
      "easy":   [ { "id": "js-easy-1", "title": "...", "description": "...", "template": "..." }, ... ],
      "medium": [ ... ],
      "hard":   [ ... ],
  }
  ```

* `TEST_BANK_JS.py` / `TEST_BANK_PY.py` — для каждой задачи:

  ```python
  TEST_BANK_JS = {
      "js-easy-1": {
          "visible": [
              { "input": [[3,1,2]], "expected": [1,2,3] },
              ...
          ],
          "hidden": [
              ...
          ]
      },
      ...
  }
  ```

`tests_runner.py` берёт `task_id`, код кандидата и соответствующие тесты, прогоняет их через песочницу и возвращает нормализованный список результатов, который отображается фронтом.

---

## 🎨 Интерфейс Workspace

`frontend/src/components/Workspace.tsx` реализует весь сценарий:

* выбор трека (JS / Python) через кнопки в чате
* отображение карточки текущего задания
* чат с ИИ-рекрутером (аватарки, typing-индикатор, встроенные кнопки)
* Monaco Editor с подсветкой JS/Python
* блок **Логи** (stdout / stderr песочницы)
* блок **Результаты тестов**:

  * видимые тесты поштучно
  * счётчик скрытых тестов

### Soft-режим

Когда начинается soft-интервью:

* редактор кода скрывается
* справа отображается **карточка soft-skills вопроса** с подсказками
* внизу в чате остаётся только textarea для ответа и кнопка «Ответить»
* в блоке тестов выводится пояснение: для soft-skills автотестов нет

Это помогает кандидату не путаться: техническая часть и soft-часть визуально разные.

---

## ✅ Краткий чек-лист для запуска

1. Клонировать репозиторий.
2. Настроить backend:

   * `cd backend`
   * `python -m venv .venv && activate`
   * `pip install -r requirements.txt`
   * создать `.env` с ключами LLM
   * `uvicorn main:app --host 0.0.0.0 --port 8000 --reload`
3. Настроить frontend:

   * `cd frontend`
   * `npm install`
   * настроить прокси `/api` → `http://localhost:8000` в `vite.config.*` (если ещё не настроен)
   * `npm run dev`
4. Открыть браузер: `http://localhost:5173`
5. Выбрать язык интервью (JS / Python) и пройти полный сценарий.
6. После завершения soft-блока PDF-отчёт будет лежать в `backend/reports/`.

Если нужен контейнер — использовать описанные выше Docker/Docker Compose команды.
